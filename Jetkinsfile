pipeline {
    agent any

    stages {
        stage('Clone Stage') {
            steps {
                // Cloner le dépôt Git
                git 'https://github.com/ndoreginald/data_docker_angular.git'
            }
        }
    }
}
